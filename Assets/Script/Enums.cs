public enum TopoTags
{
    Untagged,
    wall,
    floor,
    pillar,
    stair,
}

public enum PlayerState
{
    Move,
    Crouch,
}

public enum EnemyState
{
    Move,
    Attack,
    Run
}

public enum EnemyType
{
    Human,
    Monster
}